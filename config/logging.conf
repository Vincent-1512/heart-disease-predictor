[loggers]
keys=root

[handlers]
keys=console

[formatters]
keys=basic

[logger_root]
level=INFO
handlers=console

[handler_console]
class=StreamHandler
level=INFO
formatter=basic
args=(sys.stdout,)

[formatter_basic]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s
